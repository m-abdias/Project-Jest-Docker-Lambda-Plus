service: tooling-jest-docker-plus
frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs16.x

plugins: 
 - serverless-offline

functions:
  hello:
    handler: ./src/index.main
    events:
      - httpApi:
          path: /
          method: get
